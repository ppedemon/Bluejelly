#! /bin/bash

BASE=`dirname ${0}`

CP=
for jar in `ls ${BASE}/lib/*.jar`; do
  CP+=":"$jar
done
CP=${CP:1}

java -Dapp.version=${build.version} -Dapp.name=${build.appname} -cp ${CP} bluejelly.l4.L4C $@
exit $?
